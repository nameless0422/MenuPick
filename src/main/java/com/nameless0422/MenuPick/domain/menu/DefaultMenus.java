package com.nameless0422.MenuPick.domain.menu;

import java.util.List;

/**
 * 새 계정이 처음부터 갖고 있는 기본 메뉴 목록.
 *
 * <p><b>왜 필요한가.</b> 가입 직후 메뉴 목록은 비어 있고, 이 앱에서 사용자가 가장 먼저 하고 싶은
 * 일(픽)은 메뉴가 하나도 없으면 아예 동작하지 않는다. 즉 처음 들어온 사람은 "뭘 먹을지 골라주는
 * 앱"에 들어와서 <b>먼저 20여 개를 직접 입력해야</b> 한다. 미로그인 데모({@link
 * com.nameless0422.MenuPick.domain.pick.DemoPickService})가 곧바로 결과를 보여주고 나서
 * 가입하면 빈 화면이 나오는 것이라, 온보딩 퍼널이 정확히 그 지점에서 끊긴다.
 *
 * <p><b>어휘를 맞춘 곳.</b> 카테고리는 화면 프리셋({@code frontend/src/constants.ts}의
 * {@code CATEGORY_PRESETS})과 같은 값만 쓴다. 백엔드에서 카테고리는 자유 문자열(VARCHAR 20)이라
 * 아무 값이나 들어갈 수 있지만, 프리셋에 없는 값을 시드로 넣으면 사용자가 필터 화면에서 고를 수
 * 없는 카테고리가 자기 데이터에만 존재하게 된다. 메뉴 이름도 데모 샘플과 겹치게 골라, 가입 전에
 * 본 것과 가입 후에 보이는 것이 이어지도록 했다.
 *
 * <p><b>기본값의 성격.</b> 이건 제안이지 규칙이 아니다 — 전부 평범한 사용자 소유 메뉴로 들어가고
 * (가중치 1, 제외 아님), 사용자가 이름을 고치든 지우든 아무 제약이 없다. 시드인지 아닌지를
 * 구분하는 플래그를 두지 않는 이유가 이것이다. 플래그가 있으면 "기본 메뉴는 지울 수 없다"거나
 * "업데이트 때 다시 채운다" 같은 규칙이 따라붙기 마련인데, 남의 개인 목록을 앱이 나중에
 * 손대는 동작은 이 앱에 없어야 한다.
 *
 * <p><b>목록을 바꿀 때.</b> 여기를 고치면 <b>앞으로 가입하는 계정만</b> 바뀐다. 이미 만들어진
 * 계정은 {@code V10__seed_default_menus.sql}이 한 번 넣어 준 그때의 목록을 갖고 있고, 마이그레이션
 * 파일은 불변이라({@code MigrationImmutabilityTest}) 두 목록은 시간이 지나면 갈라진다. 그건
 * 의도한 것이다 — 이미 자기 목록을 손질한 사용자의 데이터를 나중에 맞춰 넣을 이유가 없다.
 */
public final class DefaultMenus {

    /**
     * 기본 메뉴 한 건. 카테고리를 하나만 갖는 이유는 시드가 분류의 정답을 정하는 자리가 아니기
     * 때문이다 — 예를 들어 치킨은 한식으로도 패스트푸드로도 볼 수 있고, 그건 사용자가 나중에
     * 자기 기준으로 고치면 되는 값이다.
     */
    public record Preset(String name, String category) {}

    /**
     * 프리셋 8종 중 7종을 덮는다. "카페·디저트"는 넣지 않았다 — 이 앱이 뽑아 주는 것은 끼니이고,
     * 후식은 끼니와 같은 후보 풀에 섞이면 픽 결과의 성격이 흐려진다. 필요한 사람은 직접 추가한다.
     *
     * <p>22개인 것은 "가중치를 만지지 않아도 결과가 지루하지 않은 최소치"에 맞춘 것이다. 너무
     * 적으면 같은 메뉴가 반복해서 나오고, 너무 많으면 처음 열었을 때 목록 정리부터 해야 한다.
     */
    public static final List<Preset> PRESETS = List.of(
            new Preset("김치찌개", "한식"),
            new Preset("된장찌개", "한식"),
            new Preset("제육볶음", "한식"),
            new Preset("비빔밥", "한식"),
            new Preset("삼겹살", "한식"),
            new Preset("냉면", "한식"),

            new Preset("짜장면", "중식"),
            new Preset("짬뽕", "중식"),
            new Preset("탕수육", "중식"),
            new Preset("마라탕", "중식"),

            new Preset("초밥", "일식"),
            new Preset("돈까스", "일식"),
            new Preset("라멘", "일식"),

            new Preset("파스타", "양식"),
            new Preset("피자", "양식"),
            new Preset("스테이크", "양식"),

            new Preset("떡볶이", "분식"),
            new Preset("김밥", "분식"),

            new Preset("쌀국수", "아시안"),
            new Preset("팟타이", "아시안"),

            new Preset("햄버거", "패스트푸드"),
            new Preset("치킨", "패스트푸드")
    );

    private DefaultMenus() {
    }
}
