# MenuPick 현재 상태

**최종 갱신: 2026-09-23 / 기준 브랜치: `main`**

이 문서는 구현·배포·운영의 현재 상태를 빠르게 확인하는 요약이다. 세부 기능 계약은
[Specification.md](Specification.md), 설계 근거는 [DecisionLog.md](DecisionLog.md), 운영 절차는
[deploy/oci/README.md](../deploy/oci/README.md)를 따른다.

## 제품 완성도

### 2026-09-23 방문 처리 취소 — 구현, 미배포

- 히스토리에서 잘못 누른 방문 처리를 취소할 수 있다. `DELETE /history/{id}/visit`는 본인
  기록의 `isVisited`와 `visitedAt`만 되돌린다. 픽 기록, 고른 식당, 추천 피드백은 유지한다.
- 취소 후 히스토리 목록·방문 달력·식사 기록 요약을 다시 읽는다. DB 마이그레이션과 기능
  플래그는 없다.

### 2026-09-21 태그 빠른 선택 — 운영 배포 완료

- 픽 화면의 태그 필터가 자동완성뿐이라, 이름을 이미 아는 사람만 쓸 수 있었다(키워드가 비면
  서버가 빈 목록을 준다). 전날 깐 기본 태그 6개도 화면에 보이지 않았다.
- 타이핑 전에는 내 태그를 최대 8개까지 칩으로 보여주고, 치기 시작하면 검색 결과로 바뀐다.
  태그가 하나도 없으면 설정 링크를 안내한다. 프론트 전용 변경이다([D-050](DecisionLog.md) 후속).
- 프론트 lint·build, Vitest 39개 파일 444개, Playwright 3/3 통과.
- PR #295·#296으로 병합해 `main` SHA `ec5a917a2cab98d71ca29ba9338193055525b3da`로 2026-09-21 운영 배포함. 배포 전 DB 백업,
  app·web healthy, HTTPS 200, 배포 번들에 안내 문구 포함, ERROR 0건을 확인함.

### 2026-09-21 기본 태그 — 운영 배포 완료

- 운영 태그 수가 0개라 태그를 쓰는 기능 넷이 잠들어 있었다. 가입 시 태그 6개를 만들고 기본
  메뉴에 연결한다. 기존 계정은 V16이 한 번 백필한다([D-050](DecisionLog.md)).
- 백엔드 check 837개(실패 0), 프론트 Vitest 441개, Playwright 3/3 통과.
- PR #291·#292로 병합해 `main` SHA `8860f3382054d46cff700dcc5947466071518ba6`로 2026-09-21 운영 배포함. 배포 전 DB 백업
  (`gzip -t` 통과), Flyway 16 적용 성공, app·web healthy, HTTPS 200, ERROR 0건을 확인함.
- 백필 결과: 인증 계정 2개에 태그 6개씩·연결 38건씩 들어갔고, 미인증 계정 1개는 대상에서
  빠져 0개로 남았음(의도대로).

### 2026-09-20 첫 사용자 안내 — 운영 배포 완료

- 가입 직후 픽 화면에서 기본 메뉴 22개 중 안 먹는 것을 빼게 한다. 첫 추천부터 취향이 반영된다.
- 완료 상태를 저장하지 않고 픽 기록·제외 메뉴로 판정한다([D-049](DecisionLog.md)).
  `GET /onboarding`과 `PATCH /menus/exclusions` 추가. DB 마이그레이션·기능 플래그는 없다.
- 백엔드 check 829개(실패 0), 프론트 lint·build, Vitest 39개 파일 441개, Playwright 3/3 통과.
- PR #285·#286으로 병합해 `main` SHA `546bf802bd0dd7015f82e79a6d5d4ed678539b15`로 2026-09-20 운영 배포했다. 배포 전 DB 백업
  (`gzip -t` 통과), app·web healthy, readiness UP, HTTPS 200, 새 API 둘 다 unauth 401,
  배포 번들에 안내 문구 포함, 기동 후 ERROR 0건을 확인했다.
  기존 계정은 이미 픽 기록이 있어 안내가 뜨지 않는다 — 실제 노출은 다음 신규 가입에서 확인된다.

### 2026-09-19 여럿이 같이 뽑기 — 운영 배포 완료

- 호스트가 방을 만들어 링크를 공유하면, 받은 사람은 **가입 없이** 들어와 못 먹는 메뉴를 빼고
  다 같이 하나를 뽑는다. 결과는 한 번만 정해지고 호스트 히스토리에 남는다([D-048](DecisionLog.md)).
- V15 마이그레이션(`pick_rooms`·`pick_room_menus`·`pick_room_vetoes`) 추가. 기능 플래그는 없다.
- 백엔드 check 819개(실패 0), 프론트 lint·build, Vitest 38개 파일 433개(KST·UTC 양쪽), Playwright 3/3 통과.
- PR #281·#282로 병합해 `main` SHA `a25ab68ae3c3aa00496479607d6a90ea5ea2f678`로 2026-09-19 운영 배포했다. 배포 전 DB 백업
  (`gzip -t` 통과), Flyway 15 적용 성공과 세 테이블 생성, app·web healthy, HTTPS 200,
  방 만들기 unauth 401 / 없는 방 404, 기동 후 ERROR 0건을 확인했다.
  로그인 상태로 방을 만들어 링크를 여는 흐름은 아직 실제 브라우저에서 확인하지 않았다.

### 2026-09-19 내 식사 기록 요약 — 운영 배포 완료

- 히스토리 화면 맨 위에 최근 30일 픽 수·먹은 수, 많이 먹은 카테고리·메뉴, 오래 안 뽑힌 메뉴를
  보여준다. 집단 통계와 달리 최소 인원 문턱이 없다 — 내 기록이기 때문이다([D-047](DecisionLog.md)).
- `GET /history/summary` 추가. DB 마이그레이션·기능 플래그는 없다.
- 백엔드 check 797개(실패 0), 프론트 lint·build, Vitest 36개 파일 420개, Playwright 3/3 통과.
- PR #276·#278(시간대 수정)·#277로 병합해 `main` SHA `a2f73f8f8aeb51c2051f7e4b50b12938451b8995`로 2026-09-19 운영 배포했다.
  배포 전 DB 백업(`gzip -t` 통과), app·web healthy, readiness UP, HTTPS 200, 새 API unauth 401,
  배포 번들 포함, 기동 후 ERROR 0건을 확인했다.
- **사고 기록**: #276은 CI frontend 실패 상태로 머지됐다(머지 전 확인을 건너뛴 실수). 원인은
  서버 시각을 브라우저 지역시간으로 읽던 코드로, KST 고정 해석(`kstTime.ts`)으로 #278에서
  고친 뒤 배포했다. 운영에 실패 상태가 나간 적은 없다.

### 2026-09-18 픽 결과 공유 — 운영 배포 완료

- 사용자 3명, 마지막 픽 9월 13일. 앱 안 기능보다 유입 경로가 필요해 공유를 붙였다.
- 결과 카드의 "공유하기" → 공유 시트 → 클립보드 → 직접 복사 순으로 내려간다. 서버 호출·
  DB 변경·기능 플래그는 없다([D-046](DecisionLog.md)).
- 프론트 lint·build, Vitest 35개 파일 414개, Playwright 3/3 통과.
- PR #272·#273으로 병합해 `main` SHA `8695d0b06d0fa4456b1bc8d5cbe2a5297f9768e5`로 2026-09-18 운영 배포했다. 배포 전 DB 백업
  (`gzip -t` 통과), app·web healthy, readiness UP, HTTPS 200, 배포된 HTML의 OG 태그와 번들의
  공유 버튼 포함, 기동 후 ERROR 0건을 확인했다. 실제 기기에서 공유 시트를 띄워 보지는 않았다.
- **한계**: 링크를 받은 사람은 자체 서명 인증서 경고를 먼저 본다. 도메인·정식 인증서가 없는 한
  공유·PWA·클립보드가 모두 제약을 받는다. 다음 후보 작업이다.

### 2026-09-17 픽 결과에서 주변 식당 고르기 — 운영 배포 완료

- 운영 기준 저장 식당 1곳·메뉴 연결 1건이라 거리로 뽑기와 결과 지도가 사실상 비어 있었다.
- 결과 카드에서 "근처 ○○ 식당 찾기" → 카카오 검색(반경 2km, 5곳) → "여기서 먹을게요"를 누르면
  `POST /history/{id}/place`가 식당 저장·메뉴 연결·픽 기록을 한 트랜잭션으로 처리한다.
  방문 처리는 하지 않는다([D-045](DecisionLog.md)). DB 마이그레이션·기능 플래그는 없다.
- 백엔드 check 784개(실패 0), Vitest 34개 파일 407개, Playwright 3/3 통과.
- PR #268·#269로 병합해 `main` SHA `741d8aede612a9183994ee9d8e3b816a6d637c6c`로 2026-09-17 운영 배포했다. 배포 전 DB 백업
  (`gzip -t` 통과), app·web healthy, readiness UP, Flyway 14, HTTPS 200, HTTP 301, 새 API unauth 401,
  배포 번들 포함, 기동 후 ERROR 0건을 확인했다. 운영 카카오 키로 검색 결과를 화면에서 직접 확인하지는 않았다.

### 2026-09-16 지난번 픽 방문 확인 — 운영 배포 완료

- 운영 30일 기준 픽 10회에 방문 처리·수락·거절이 모두 0건이라, 그 기록을 쓰는 방문 캘린더·
  집단 경향·개인화 보정·방문율 KPI가 전부 빈 상태였다.
- 픽 화면을 열면 뽑은 지 30분~48시간 된 가장 최근 픽 하나를 두고 "○○ 드셨어요?"를 묻는다.
  "먹었어요"는 기존 방문 처리 API, "안 먹었어요"는 브라우저에만 기억한다([D-044](DecisionLog.md)).
- `GET /history` 항목에 `recommendationFeedback`을 추가했다. DB 마이그레이션·기능 플래그는 없다.
- 백엔드 check 773개(실패 0), 프론트 lint·build, Vitest 33개 파일 398개, Playwright 3/3 통과.
- PR #264·#265로 병합해 `main` SHA `a4f6b775afb1e34e34a797422aa75f73c6a90696`로 2026-09-16 운영 배포했다. 배포 전 DB 백업
  (`gzip -t` 통과), app·web healthy, readiness UP, Flyway 현재 버전 14, HTTPS 200, HTTP 301,
  unauth API 401, 배포 번들의 질문 문구 포함을 확인했다. 성공 지표(방문 건수)는 아직 측정 전이다.

### 2026-09-15 방문 캘린더 — 운영 배포 완료

- 인증 사용자의 `visited=true` 기록을 월 단위로 읽는 API와 semantic table 화면을 구현했다.
- 날짜 의미는 실제 식사일이 아니라 **‘방문했어요를 누른 날짜’**다.
- KST 현재 월 기본값, `2000-01` 하한, 미래 월 400을 적용한다. 초과 시 최신 500건을
  시간 오름차순으로 반환하고 `truncated`와 ‘최근 500개’ 안내를 제공한다.
- 기존 히스토리 이름 보존 정책을 재사용하며 DB 마이그레이션, 좌표·필터 조건 조회,
  기존 히스토리 목록 변경은 없다.
- 백엔드 773개(실패 0), MySQL query budget 1, JaCoCo instruction 93%·branch 81%,
  Vitest 31개 파일 376개, lint·build, Playwright 3/3을 통과했고 Astra blocker는 0건이다.
- PR #260·#261로 병합해 `main` SHA `bc518a5856fcde8236a2e2c66cfa16b50edc8f91`로 2026-09-15 운영 배포했다.
  배포 전 DB 백업(`gzip -t` 통과), app·web healthy, Flyway 현재 버전 14(신규 마이그레이션 없음),
  HTTPS 200, unauth API 401, 배포 번들의 캘린더 코드 포함을 확인했다. 기능 플래그는 없다.
- 실사용 성공 지표는 아직 측정하지 않았다. 상세는
  [VisitCalendarDesign.md](VisitCalendarDesign.md)를 따른다.

- 이메일·카카오·구글 로그인, 메일 인증, 비밀번호 재설정, 계정 연동·탈퇴·복구 구현 완료
- 메뉴·카테고리·태그·식당 관리와 메뉴-식당 연결, 별점·메모 구현 완료
- 카테고리·포함/제외 태그·거리 조건을 적용한 가중 랜덤 픽 구현 완료
- 픽 히스토리, 방문 처리, 필터 조건 기록 구현 완료
- 로그인부터 설정까지 주요 React 화면과 접근성 보완 구현 완료
- 게스트 데모 픽 구현 완료
- 가입→메뉴 등록→식당 저장·연결→픽→방문 처리 핵심 여정을 Playwright Chromium으로 검증

### 2026-09-13 최근 집단 경향

- 코드와 Flyway V14는 `main` 커밋 `c4e0bd7`로 운영 배포를 완료했다. 다만 운영의
  `TRENDS_ENABLED`는 기본값 `false`를 유지하므로 사용자 화면에는 아직 노출되지 않는다.
- 인증 사용자에게 canonical 메뉴·카테고리별 최근 선택·방문 사용자 순위를 제공한다.
- 기본은 `trends.enabled=false`이며 disabled 상태에서 DB 스캔·잠금을 하지 않는다.
- 기본값은 7일·5명·차원별 10개다. 식별자가 없어도 익명성은 보장되지 않으므로 문턱을
  임의로 낮추지 않는다.
- 동시 재집계는 singleton 잠금으로 직렬화하고 실패 시 이전 스냅샷을 롤백해 보존한다.
- `POST /actuator/trends`는 관리 포트 전용이며, 탈퇴 반영은 최대 36시간 지연될 수 있다.

### 2026-09-06 추천 품질 개선

- 최근 3일에 추천된 메뉴는 대체 후보가 있을 때 우선 제외한다.
- 모든 후보가 최근 추천 메뉴라면 원래 후보로 폴백해 추천 자체가 막히지 않는다.
- 픽 결과에 선호도, 카테고리·태그·거리 조건, 최근 미추천 여부를 추천 이유로 표시한다.
- 구버전 API 응답에는 추천 이유를 빈 목록으로 처리해 배포 전환 중에도 화면이 깨지지 않는다.
- 픽 직후 `이걸로 먹을래요`·`별로예요` 피드백을 히스토리에 별도 기록한다. 실제 방문 기록과
  섞지 않아 추천 직후의 선택 의도를 보존한다.

### 2026-09-07 피드백 기반 개인화

- 최근 30일간 메뉴별 긍정·부정 피드백의 차이를 추천 시 유효 가중치에 반영한다.
- 한 메뉴의 보정 폭은 ±2, 최종 유효 가중치는 최소 1로 제한해 소량의 피드백이 추천을
  과도하게 지배하거나 메뉴를 완전히 배제하지 않게 했다.
- 사용자가 직접 설정한 기본 선호도(`menus.weight`)는 변경하지 않는다.
- 보정이 적용된 결과에는 `최근 30일간 선택 피드백을 반영했어요`라는 추천 이유를 표시한다.

### 2026-09-08 기본 제외 태그

- 설정에서 알레르기·기피 태그를 기본 제외 조건으로 저장할 수 있다.
- 픽 요청이 제외 태그를 생략하면 서버가 기본값을 적용하므로 다른 클라이언트에서도 안전하다.
- 픽 화면은 기본값을 먼저 보여주며, 빈 배열을 명시해 해당 픽에서만 전부 해제할 수 있다.

### 2026-09-08 핵심 사용자 여정 E2E

- 실제 Chromium에서 가입부터 방문 처리까지 여섯 화면의 연결을 자동 검증한다.
- 상태 기반 API 대역을 사용해 앞 단계가 완료되지 않으면 픽이 성공하지 않도록 했다.
- 실제 백엔드·MySQL 계약은 기존 통합 테스트가 담당하고, 외부 카카오·메일 장애는 브라우저
  테스트에서 분리한다. 범위와 실행법은 [E2ETestPlan.md](E2ETestPlan.md)에 정리했다.

### 2026-09-11 픽 개인화 조회 통합

- 픽마다 따로 실행하던 최근 3일 추천 메뉴 조회 1건과 최근 30일 긍정·부정 피드백 조회
  2건을 메뉴별 조건부 집계 1건으로 합쳤다.
- 집계 결과는 피드백 이벤트별 행이 아니라 메뉴별 한 행이며, 최신 추천 시각과
  `ACCEPTED - REJECTED` 합계를 함께 돌려준다. 합계를 먼저 상쇄한 뒤 기존과 같이 ±2로
  제한하므로 최근 중복 회피·폴백·추천 이유·유효 가중치 의미는 바뀌지 않는다.
- 이 변경은 요청당 히스토리 조회를 3건에서 1건으로 줄인 코드 수준 최적화다. 운영 환경을
  건드리거나 부하 시험을 다시 하지 않았으므로 지연시간·처리량 개선 수치는 주장하지 않는다.

## 성능·배포

- 2026-09-04 실제 부하 시험에서 일반 100 rps 구간을 통과했고 무릎은 약 450~500 rps에서
  확인했다. 상세 수치는 [LoadTestResults.md](LoadTestResults.md)에 기록돼 있다.
- nginx `worker_connections`는 1,024에서 4,096으로 올렸고 컨테이너 `nofile`은 65,535다.
- 프론트·백엔드 이미지는 CI가 `linux/amd64`, `linux/arm64`로 GHCR에 게시한다.
- Hikari 최대 풀 크기는 `DB_MAX_POOL_SIZE`로 외부화했다. **2026-09-10에 10→16 A/B를
  실행했고 16을 채택하지 않았다** — 차이(+3.9%)가 회차 간 흔들림(±39%)보다 작았다.
  운영값은 10이다([LoadTestResults.md](LoadTestResults.md) 2차 회차).

## 서버 보안

2026-09-05 접근 로그 점검에서 공개 SSH를 향한 무작위 계정 스캔 978건과 `/.env` 웹 스캔을
확인했지만, 알 수 없는 IP의 로그인 성공·환경파일 노출·애플리케이션 침입 흔적은 없었다.

- SSH는 `opc` 공개키 로그인만 허용한다.
- 비밀번호·키보드 대화식 인증과 root 로그인을 차단했고 `MaxAuthTries`는 3이다.
- fail2ban이 10분 내 3회 실패한 IP를 1시간 차단하며 반복 시 최대 1일까지 늘린다.
- nginx는 `/.env`, `/.git` 등 숨김파일 경로를 404로 반환한다.
- MySQL·Redis·애플리케이션·Mailpit 관리 포트는 인터넷에 직접 공개하지 않는다.
- 커밋 `120095d`, PR #220(dev)·#221(main), main CI run `33968672848` 통과.

## 현재 운영 상태

**2026-09-09 재기동 완료.** 컨테이너 5개 모두 실행 중이며 `9e7b695`(#236)를 서비스한다.

| 컨테이너 | 상태 |
| --- | --- |
| `menupick-app` | 실행 (`9e7b695`) |
| `menupick-web` | 실행 (`9e7b695`) |
| `menupick-mysql` | 실행 유지 |
| `menupick-redis` | 실행 유지 |
| `menupick-mailpit` | 실행 유지 |

2026-09-05부터 9/9까지는 app/web을 내려 둔 상태였다(개발 중이라 의도적으로 중지). 재기동
시점에 `becb317`(#221)에서 `9e7b695`(#236)로 15개 PR 분을 한 번에 올렸고, 그 과정에서
**V11·V12가 운영 DB에 처음 적용됐다** — `histories`에 `is_visited`·`recommendation_feedback`
추가, `user_default_excluded_tags` 생성. 둘 다 `success=1`이고 기존 데이터는 그대로였다
(users 2 / menus 23 / restaurants 1 / links 1 / histories 1, 배포 전후 동일).

재기동 검증 항목은 모두 통과했다 — Flyway 적용, readiness `UP`, HTTPS 200, `:80` → 301,
숨김파일(`/.env`, `/.git/*`) 404, 그리고 컨테이너 재생성 후에도 유지되는 nginx
`worker_connections 4096` / `nofile 65535`.

### 메일 발송 — 개발 서버는 Mailpit

`MAIL_HOST=mailpit`, `MAIL_PORT=1025`. 실제 공급자를 가리키던 것을 2026-09-09에 바꿨다.
테스트 계정을 만들 때마다 진짜 메일이 밖으로 나가고, 존재하지 않는 주소로는 인증 링크를
받을 방법이 아예 없기 때문이다(토큰은 Redis에 해시로만 저장돼 서버에서 원문을 꺼낼 수 없다).
받은 메일은 `http://localhost:8025`(SSH 터널)에서 본다.

**정식 서비스 전환 시 실제 SMTP 공급자로 되돌릴 것.** 그대로 두면 가입자가 인증 메일을
영영 못 받는데 앱은 정상 기동하므로 아무도 알아채지 못한다. 근거와 절차는
[.env.prod.example](../.env.prod.example)의 MAIL 절에 함께 적어 뒀다.

### 2026-09-09 KPI 집계

- 방문율·재픽률·7일 리텐션을 `GET /actuator/kpi`가 실제로 계산한다(관리 포트 전용).
- 세 지표 모두 비율과 함께 분자·분모를 준다. 분모가 0이면 비율은 `null`이다 —
  "표본 없음"을 `0.0`으로 채우면 "성과 나쁨"으로 읽힌다.
- 7일 리텐션은 첫 픽 후 7일이 지나지 않은 사용자를 분모에서 뺀다. 넣으면 신규 가입이
  늘수록 리텐션이 떨어지는 착시가 생긴다.
- 기본 집계 기간 30일은 개인화 보정이 보는 창과 맞춘 값이다.

### 2026-09-12 픽 쿼리 예산 고정

- 픽 한 번이 던지는 JDBC 구문 수를 테스트로 못 박았다 — 필터 없음 **7건**, 필터 있음 **12건**.
- 후보를 3배로 늘려도 조회가 비례해 늘지 않는 것(N+1 부재)도 함께 고정했다.
- 왜 필요했나: 추천 중복 완화·피드백 보정·기본 제외 태그가 각각 조회를 하나씩 얹었는데
  **기능별 테스트는 전부 초록불이었고**, 조회가 늘어난 사실은 2026-09-10 부하 시험에서야
  드러났다. 이제 같은 일이 생기면 CI에서 걸린다.
- 필터 픽 12건 중 `history_filter_conditions` INSERT 3건이 한 번으로 묶이지 않는데, 배치
  설정 문제가 아니라 **IDENTITY 키 생성** 때문이다(생성된 키를 받으려 즉시 실행해야 해
  JDBC 배치가 성립하지 않는다). 줄이려면 키 전략을 바꿔야 하고 그건 마이그레이션이라
  여기서 다루지 않았다.

### 2026-09-13 상황별 빠른 픽 (프리셋)

자주 쓰는 픽 조건에 이름을 붙여 저장하고 다시 실행한다(사용자당 10개). 설계는
[PickPresetDesign.md](PickPresetDesign.md), 결정 근거는 [D-039](DecisionLog.md)·[D-040](DecisionLog.md).

- 저장하는 것은 카테고리·포함 태그·**추가** 제외 태그·거리뿐이다.
- **좌표를 저장하지 않는다.** 거리 기준점은 실행할 때마다 브라우저에서 새로 받는다 —
  회사에서 만든 프리셋을 집에서 눌렀을 때 회사 주변을 뽑지 않기 위해서다.
- **기본 제외 태그를 복사하지 않고** 실행할 때마다 최신 값을 합친다. 프리셋이 기본 제외를
  **해제할 수단은 없다**. 유효 제외가 50개를 넘으면 자르지 않고 409로 멈춘다.
- **참조 태그가 지워지면 실행이 막힌다**(`needsReview`, 버전도 함께 오른다). 사용자가 전체
  조건을 다시 확인해 저장해야 풀린다 — 조건이 조용히 넓어진 채로 추천이 나가지 않게.
- 실행은 전용 경로 `POST /pick/presets/{id}/pick`이고 필터 override를 받지 않는다.
  일반 `POST /pick`의 계약(명시 제외 20 상한, null/빈 배열 의미)과 **쿼리 예산 7/12는 그대로다.**
  빠른 픽 실행은 별도 예산 14로 따로 고정했다.
- `main` 커밋 `c4e0bd7`로 **운영 배포를 완료했다**(2026-09-13).

## 다음 제품 과제

**후보 없음 조건 조정 대안 운영 활성화:** 수동 픽이 `NO_PICK_CANDIDATES`로 끝날 때
안전한 조건 조정안을 제시하는 API와 프론트를 `main` SHA
`165b154b6c653584a42b2ab260f320c2efb785e9`로 2026-09-14 운영 배포했다. DB 마이그레이션은 없다.
코드 기본값은 백엔드·프론트 모두 `false`로 유지하며, 운영에서는 두 플래그를 `true`로 맞췄다. 상세 계약은
[PickAlternativesDesign.md](PickAlternativesDesign.md)를 따른다. 롤아웃 성공 지표 베이스라인은
아직 없다.

구현 검증은 완료했다. 백엔드 full check는 764 tests / 0 failures, JaCoCo instruction 93% /
branch 81%였고, 프론트는 lint·build, Vitest 29 files / 363 tests, Playwright 3/3을 통과했다.
stale failed-request race 회귀 수정 후 최종 Astra 리뷰 blocker도 0건이다. PR #254~#257의
CI와 운영 smoke도 통과했다(health/readiness UP, HTTPS 200, HTTP 301, unauth API 401, arm64).

1. ~~정의만 있는 방문율·재픽률·7일 리텐션 KPI의 실제 집계 수단을 만든다.~~
   **2026-09-09 완료** — `GET /actuator/kpi`([Specification.md 8장](Specification.md#8-추천-품질-kpi)).
2. 즉시 피드백 보정이 수락률을 높이는지 측정한 뒤 실제 방문 이력 반영 여부를 결정한다.
   이제 1번이 생겨 측정이 가능하다. 다만 **표본이 아직 없다** — 실사용자가 붙기 전에는
   세 지표 모두 분모가 0에 가깝다. 공개 시점 이후에 판단할 항목이다.
3. ~~`DB_MAX_POOL_SIZE` 10→16 A/B 시험.~~ **2026-09-10 실행, 채택하지 않음.**
   워밍 조건을 맞추면 16이 +3.9%(392→407 rps)지만 워밍업 유무만으로 ±39%가 흔들리는
   환경이라 노이즈 안이다. 서버는 10을 유지한다. 대신 **요청당 DB 왕복을 줄이는 쪽**이
   본 문제로 드러났다 — 아래 5번.
5. ~~**픽 한 번의 조회 수를 줄인다.**~~ **2026-09-11 1차 완료 — 개인화 히스토리 조회
   3건을 메뉴별 집계 1건으로 통합.** 저부하(100 rps)에서는 P95 27ms로 1차 회차(61ms)보다
   빨라졌는데, 450 rps에서는 267ms → 2.05s로 느려졌다. 중복 완화·피드백 보정·기본 제외
   태그가 각각 던지는 조회가 풀(10)이 포화되는 구간에서 처리량을 깎는다. 목표 부하는
   여유롭게 통과하므로 급하지는 않다([LoadTestResults.md](LoadTestResults.md) 2차 회차).
4. 부하 시험의 `heavy` 프로파일(메뉴 수 민감도)과 `soak`(누수)은 미실행이다.
